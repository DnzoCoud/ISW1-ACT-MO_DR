spring:
  application:
    name: financial-system
  datasource:
    url: jdbc:postgresql://postgres:5432/financiero_db
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
server:
  port: 8080
